# Зависимости для ноутбука. Он рассчитан на среду Kaggle, где всё уже
# предустановлено; локально достаточно совместимых версий из диапазонов ниже.
#
# Прогон с результатами в REPORT.md выполнен на Kaggle:
#   Python 3.12, torch 2.10.0+cu128, transformers 5.0.0, datasets 4.8.5, nltk 3.9.1
#
# Ноутбук версионно-устойчив: имена аргументов Trainer
# (eval_strategy / evaluation_strategy и processing_class / tokenizer)
# выбираются по сигнатуре, поэтому он работает и на transformers 4.40+, и на 5.x.
torch>=2.2
transformers>=4.40
datasets>=2.18
nltk>=3.9
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
