# Legacy Scripts Archive

These scripts are no longer active entry points after the project refactor.
They are kept for traceability and can be restored if an older experiment is needed.

Active scripts now live in:

- `scripts/common/`
- `scripts/football/`
- `scripts/nba/`
